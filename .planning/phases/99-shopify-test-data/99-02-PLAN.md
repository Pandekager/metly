---
phase: 99-shopify-test-data
plan: "02"
type: execute
wave: 2
depends_on:
  - "99-01"
files_created:
  - backend/SCHEDULE.md
files_modified:
  - backend/main.py
autonomous: true
requirements: []

must_haves:
  truths:
    - "Test data runs daily via cron"
    - "Test data integrates with existing populateDB flow"
  artifacts:
    - path: "backend/main.py"
      provides: "CLI entry point for test data generation"
      contains: "generateTestData"
    - path: "backend/src/scripts/generateTestData.py"
      provides: "CLI integration with main.py"
      exports: ["cli"]
  key_links:
    - from: "generateTestData.py"
      to: "populateDB.py"
      via: "import and call pattern"
      pattern: "from.*populateDB.*import"
---

<objective>
Integrate test data generation with existing populateDB flow and set up cron schedule.

Purpose: Enable scheduled daily execution
Output: Updated main.py to include test data option, cron setup documentation
</objective>

<context>
@backend/src/scripts/generateTestData.py
@backend/src/scripts/db/populateDB.py
@.planning/phases/99-shopify-test-data/99-CONTEXT.md

### Integration approach:
- Add CLI option to main.py for test data generation
- Document cron setup for daily execution at 00:00
- Reuse existing populateDB for syncing generated data to database
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integrate with main.py CLI</name>
  <files>backend/main.py</files>
  <action>
Add test data generation option to main.py:
1. Import generate_test_data from generateTestData
2. Add --test-data or --generate-test flag handling
3. When flag present, call generate_test_data() then run populateDB for that user
4. Get user from environment or command line (existing pattern)
  </action>
  <verify>
python backend/main.py --help | grep -i "test"</verify>
  <done>
  - main.py accepts --test-data flag
  - Running generates data and syncs to database
</done>
</task>

<task type="auto">
  <name>Task 2: Document cron setup</name>
  <files>backend/SCHEDULE.md</files>
  <action>
Create SCHEDULE.md with cron setup:
1. Document cron expression: `0 0 * * *` (midnight daily)
2. Show example crontab entry
3. Document required environment variables
4. Document integration with existing populateDB flow
5. Store name: "Metly Test Golf" per D-08
  </action>
  <verify>
cat backend/SCHEDULE.md | grep -i "cron"</verify>
  <done>
  - SCHEDULE.md exists with cron documentation
  - All decisions from CONTEXT.md implemented
</done>
</task>

</tasks>

<verification>
[Run main.py with --test-data flag]
[Verify data synced to database via populateDB]
</verification>

<success_criteria>
main.py extended with test data option, cron documentation created
</success_criteria>

<output>
After completion, create `.planning/phases/99-shopify-test-data/99-02-SUMMARY.md`
</output>