---
phase: 99-shopify-test-data
plan: "01"
type: execute
wave: 1
depends_on: []
files_created:
  - backend/src/scripts/generateTestData.py
autonomous: true
requirements: []

must_haves:
  truths:
    - "D-01: Products generated first, then orders use those products"
    - "D-02: All 6 categories: golf clubs, golf bags, golf balls, gloves, golf apparel, accessories"
    - "D-03: Customer data in Danish (names, addresses, cities)"
    - "D-04: Order values between 500-5000 DKK"
    - "D-06: Uses existing Shopify credentials from .env"
  artifacts:
    - path: "backend/src/scripts/generateTestData.py"
      provides: "Test data generation for Shopify"
      exports: ["run_test_generation"]
  key_links:
    - from: "generateTestData.py"
      to: "shopify.create_product, shopify.create_orders"
      via: "import and calls"
      pattern: "create_product|create_orders"
---

<objective>
Create test data generation module that generates Danish golf products and orders in Shopify and syncs to database.

Purpose: Enable scheduled generation of test data for development and testing
Output: New generateTestData.py script with standalone execution
</objective>

<context>
@backend/src/integrations/shopify/shopify.py
@backend/src/scripts/db/populateDB.py
@.planning/phases/99-shopify-test-data/99-CONTEXT.md

### Key existing patterns from shopify.py:
- DANISH_FIRST_NAMES, DANISH_LAST_NAMES, DANISH_CITIES already defined
- GOLF_PRODUCT_TEMPLATES with Danish product names
- create_product() creates products via REST API
- create_orders() creates paid orders via REST API

### Integration approach:
- Extend with new generator functions
- Reuse existing Shopify client functions
- Add Danish customer generation
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test data generation module</name>
  <files>backend/src/scripts/generateTestData.py</files>
  <action>
Create `backend/src/scripts/generateTestData.py` that:
1. Imports from shopify.py: create_product, create_orders, access token, shop domain
2. Reuses DANISH_FIRST_NAMES, DANISH_LAST_NAMES, DANISH_CITIES from shopify.py
3. Generates Danish customer data with random names, addresses, cities from predefined lists
4. Generates golf products mapping to categories: golf clubs (Driver, Iron Set, Putter, Wedge), golf bags (Stand Bag, Cart Bag), golf balls (Tour Pro, Practice), gloves (Herre, Dame), golf apparel (cap, sko, trøje, vest, regnjakke), accessories (håndklæde, scorekort-holder,Divot, Tee-box)
5. Generates orders with random 1-3 products per order, order total 500-5000 DKK
6. Exports functions: generate_products(count), generate_orders(products, count), generate_danish_customers(count), run_test_generation()
7. Uses argparse for CLI: --products N, --orders N, --customers N
8. Gets credentials from os.getenv: SHOPIFY_SHOP, SHOPIFY_ACCESS_TOKEN
  </action>
  <verify>
python backend/src/scripts/generateTestData.py --help 2>/dev/null && echo "Module loads successfully"</verify>
  <done>
  - generateTestData.py exists and imports correctly
  - CLI accepts --products, --orders, --customers arguments
  - Functions generate Danish data matching requirements
</done>
</task>

</tasks>

<verification>
[Run module with test parameters]
[Verify products created in Shopify store]
[Verify orders created with correct DKK values]
</verification>

<success_criteria>
Module create at backend/src/scripts/generateTestData.py with working CLI
</success_criteria>

<output>
After completion, create `.planning/phases/99-shopify-test-data/99-01-SUMMARY.md`
</output>